<Page x:Class="Wpf_Ado.Net_App.ViewStatisticsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Wpf_Ado.Net_App"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
      Title="Page2">

    <Grid Background="Blue">
        <Grid.RowDefinitions>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" MinWidth="100"/>
            <ColumnDefinition Width="5*"/>
        </Grid.ColumnDefinitions>

        <StackPanel >
            <Button  Style="{StaticResource OrangeButton}" Content="сутки"/>
            <Button  Style="{StaticResource OrangeButton}" Content="неделя"/>
            <Button  Style="{StaticResource OrangeButton}" Content="месяц"/>
        </StackPanel>

        <DataGrid Name="dataGrid" Grid.Column="1" 
                  GridLinesVisibility="All"
                  RowBackground="#50C878" AlternatingRowBackground="Yellow"
                  BorderBrush="Aqua" BorderThickness="5"
                  CanUserSortColumns="True"/>
    </Grid>
</Page>
